#include <stdio.h>

int main()
{

        int num = 0;

        scanf("%d", &num);

        if (num % 3 == 0)
        {
        printf("Number is Divisible by 3\n");
        }

        else 
        {
        printf("Number is Not Divisible by 3\n");
        }

return 0;
}
